/**
 * Breakpoint constants matching Tailwind config
 * These values should be kept in sync with tailwind.config.js screens
 */
export const BREAKPOINTS = {
  sm: 640,
  md: 768,
  lg: 1024,
} as const;
